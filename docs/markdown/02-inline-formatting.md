# Inline formatting

Plain paragraph with **bold**, *italic*, ***bold italic***, and `inline code`.

Strikethrough: ~~removed text~~.

Mixed: call `getValue()` on **MyClass** with *care*.

Escapes: \*not italic\*, \`not code\`, \\backslash.

Unicode and punctuation: café — “quotes” … em—dash.

Empty emphasis edge: ** ** and * *.

Long inline code: `const path = "/home/user/very/long/path/that/wraps/or/scrolls.ts";`
